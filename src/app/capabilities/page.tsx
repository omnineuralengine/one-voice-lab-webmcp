import type { Metadata } from "next";import Link from "next/link";import { CAPABILITIES,CAPABILITY_REGISTRY_VERSION } from "@/lib/capabilities/registry";
export const metadata:Metadata={title:"Applied Voice Lab Capabilities"};export default function Page(){return <main className="min-h-screen bg-[#03080d] p-4 text-slate-200"><div className="mx-auto max-w-6xl"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-cyan-300">{CAPABILITY_REGISTRY_VERSION}</p><h1 className="mt-2 text-2xl font-semibold text-white">Applied Voice Lab capabilities</h1><p className="mt-2 text-sm text-slate-500">Repository-backed implementation truth. Partial and planned capabilities are never presented as complete.</p><div className="mt-6 grid gap-3 md:grid-cols-2">{CAPABILITIES.map(c=><article key={c.id} className="rounded-xl border border-white/10 bg-[#071016] p-4"><div className="flex justify-between gap-2"><h2 className="font-semibold text-white">{c.name}</h2><span className="text-xs text-cyan-200">{c.implementationStatus}</span></div><p className="mt-2 text-xs leading-5 text-slate-400">{c.shortDescription}</p><p className="mt-2 text-[10px] text-slate-600">Mobile {c.mobileAvailability?"yes":"limited"} · Offline {c.offlineAvailability?"yes":"limited"} · verified {c.lastVerifiedAt}</p>{c.route?<Link className="mt-3 inline-block text-xs font-semibold text-cyan-200" href={c.route}>Open capability →</Link>:null}</article>)}</div></div></main>}
